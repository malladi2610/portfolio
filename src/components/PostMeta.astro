---
import TopicPill from "./TopicPill.astro";
const { pubDate, updatedDate, readingTime, topic } = Astro.props;
const published = new Date(pubDate);
const updated = updatedDate ? new Date(updatedDate) : null;
const format = (date) => date.toLocaleDateString("en-US", { year: "numeric", month: "short", day: "2-digit" });
---
<div class="post-meta">
  <TopicPill topic={topic} />
  <span>{format(published)}</span>
  {updated && <span>Updated {format(updated)}</span>}
  <span>{readingTime}</span>
</div>
